package com.inglo.giggle.resume.application.port.out;

import com.inglo.giggle.resume.domain.Resume;

public interface UpdateAdditionalLanguagePort {

    void updateAdditionalLanguages(Resume resume);
}
